// ScratchLink — module boilerplate. The life of the plugin is in the
// subsystem; the module itself has nothing to set up.
#include "Modules/ModuleManager.h"

IMPLEMENT_MODULE(FDefaultModuleImpl, ScratchLink)
